# Right Sidebar Today Layout — Browser Screenshots

**Status:** BLOCKED — `localhost:8082` nicht erreichbar in der Abnahme-Session (kein Dev-Server, Auth erforderlich).

Empfohlene manuelle Checks nach `npm run web`:

1. `/stationaer` — 6 Heute-Einträge, Schnellaktionen darunter ohne Überlappung
2. `/assist`, `/office`, `/pflege`, `/beratung`, `/akademie` — gleiches Layout
3. Sidebar scrollt bei langen Heute-Listen; Support-Footer bleibt unten

Screenshots hier ablegen: `stationaer-sidebar.png`, `assist-sidebar.png`, etc.
