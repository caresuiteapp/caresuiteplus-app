import { createDomainWorkflow } from './domainWorkflowFactory';

/** WP355 — Workflow & Status (clientPortal) */
export const clientPortalWorkflow = createDomainWorkflow(355, 'clientPortal', 'entwurf');
