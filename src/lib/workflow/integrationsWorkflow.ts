import { createDomainWorkflow } from './domainWorkflowFactory';

/** WP495 — Workflow & Status (integrations) */
export const integrationsWorkflow = createDomainWorkflow(495, 'integrations', 'entwurf');
