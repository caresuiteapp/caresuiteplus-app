import { createDomainWorkflow } from './domainWorkflowFactory';

/** WP255 — Workflow & Status (assistPlanning) */
export const assistPlanningWorkflow = createDomainWorkflow(255, 'assistPlanning', 'entwurf');
