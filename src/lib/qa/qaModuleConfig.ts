/**
 * QA hub live readiness.
 * Pilot-Checklisten und Coverage sind Demo-Snapshots; echtes TestRail/Jira-Sync bleibt offen.
 */
export function isQaLiveReady(): boolean {
  return false;
}

export const QA_PREPARED_MESSAGE =
  'Echte QA-Triage und Pilot-Tracking sind in Vorbereitung. Der Hub zeigt derzeit Demo-Coverage und Bug-Counts — keine Live-Syncs.';
