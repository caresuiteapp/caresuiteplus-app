import { AppointmentsListScreen } from './AppointmentsListScreen';

/** Full-width appointments list; row tap opens AppointmentDetailModal. */
export function AppointmentsAdaptiveScreen() {
  return <AppointmentsListScreen />;
}
