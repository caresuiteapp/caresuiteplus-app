import { useCallback, useEffect, useMemo, useState } from 'react';
import { Alert, Linking, Platform, Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import * as DocumentPicker from 'expo-document-picker';
import * as Location from 'expo-location';
import { CareSignatureCanvas } from '@/components/inputs/CareSignatureCanvas';
import { EmptyState, ErrorState, InfoBanner, LoadingState, PremiumBadge, PremiumButton, PremiumCard, PremiumInput, SectionPanel } from '@/components/ui';
import { usePortalActor } from '@/hooks/usePortalActor';
import { useAsyncQuery } from '@/hooks/core/useAsyncQuery';
import { PortalTabScreen } from './PortalTabScreen';
import { portalPremium } from '@/design/tokens/portalPremium';
import { careSpacing } from '@/design/tokens/spacing';
import { typography } from '@/theme';
import { TRAVEL_ROUTE_TYPE_LABELS, type TravelRouteType } from '@/types/modules/travelCompensation';
import type { LogbookPoint, LogbookTrip, LogbookVehicle } from '@/types/modules/employeeLogbook';
import { addLogbookStop, appendLogbookPoints, buildLogbookPdf, confirmLogbookDay, correctLogbookTrip, createLogbookReceipt, createLogbookTrip, finishLogbookTrip, getCurrentLogbookPoint, loadEmployeeLogbook, requestLogbookLocationPermission, saveLogbookProfile, saveLogbookVehicle, startNativeBackgroundTracking, stopNativeBackgroundTracking, uploadLogbookFile } from '@/lib/employeeLogbook';

type Tab = 'record' | 'trips' | 'receipts' | 'profile';
const routes = Object.keys(TRAVEL_ROUTE_TYPE_LABELS).filter((key) => key !== 'private_non_business') as TravelRouteType[];
const today = () => new Date().toISOString().slice(0, 10);

export function EmployeeLogbookScreen() {
  const actor = usePortalActor(); const [tab, setTab] = useState<Tab>('record');
  const [feedback, setFeedback] = useState<string | null>(null); const [busy, setBusy] = useState(false);
  const query = useAsyncQuery(useCallback(async () => {
    if (!actor.tenantId || !actor.employeeId) throw new Error('Mitarbeitendenkonto ist nicht vollständig verknüpft.');
    try {
      return { ok: true as const, data: await loadEmployeeLogbook(actor.tenantId, actor.employeeId) };
    } catch (error) {
      return { ok: false as const, error: error instanceof Error ? error.message : 'Fahrtenbuch konnte nicht geladen werden.' };
    }
  }, [actor.tenantId, actor.employeeId]), [actor.tenantId, actor.employeeId], { enabled: !!actor.tenantId && !!actor.employeeId });

  const active = query.data?.trips.find((trip) => trip.status === 'recording') ?? null;
  const [routeType, setRouteType] = useState<TravelRouteType>('home_to_client'); const [purpose, setPurpose] = useState('Anfahrt zum Einsatz');
  const [assignmentId, setAssignmentId] = useState(''); const [manualReason, setManualReason] = useState(''); const [startAddress, setStartAddress] = useState(''); const [endAddress, setEndAddress] = useState(''); const [notes, setNotes] = useState('');
  const [points, setPoints] = useState<LogbookPoint[]>([]); const [watcher, setWatcher] = useState<Location.LocationSubscription | null>(null);
  const selectedVehicle = query.data?.profile.defaultVehicleId ?? query.data?.vehicles.find((v) => v.active)?.id ?? null;

  useEffect(() => () => watcher?.remove(), [watcher]);
  async function begin() {
    if (!actor.tenantId || !actor.employeeId) return;
    setBusy(true); setFeedback(null);
    try {
      await requestLogbookLocationPermission(); const first = await getCurrentLogbookPoint();
      const trip = await createLogbookTrip({ tenantId: actor.tenantId, employeeId: actor.employeeId, vehicleId: selectedVehicle, routeType, purpose, assignmentId: assignmentId.trim() || null, manualReason: manualReason.trim() || null, startAddress });
      setPoints([first]); await appendLogbookPoints(trip.id, actor.tenantId, actor.employeeId, [first]);
      await startNativeBackgroundTracking({ tripId: trip.id, tenantId: actor.tenantId, employeeId: actor.employeeId });
      const subscription = await Location.watchPositionAsync({ accuracy: Location.Accuracy.High, distanceInterval: 20, timeInterval: 15000 }, (location) => setPoints((current) => [...current, { latitude: location.coords.latitude, longitude: location.coords.longitude, accuracy: location.coords.accuracy, altitude: location.coords.altitude, speed: location.coords.speed, heading: location.coords.heading, recordedAt: new Date(location.timestamp).toISOString() }]));
      setWatcher(subscription); await query.refresh(); setFeedback('Fahrt läuft. GPS-Aufzeichnung und Android-Hintergrunddienst sind aktiv.');
    } catch (error) { setFeedback(error instanceof Error ? error.message : 'Fahrt konnte nicht gestartet werden.'); } finally { setBusy(false); }
  }
  async function finish() {
    if (!active || !actor.tenantId || !actor.employeeId) return;
    setBusy(true); setFeedback(null);
    try {
      watcher?.remove(); setWatcher(null); await stopNativeBackgroundTracking();
      const last = await getCurrentLogbookPoint(); const finalPoints = [...points, last];
      await finishLogbookTrip(active.id, { tenantId: actor.tenantId, employeeId: actor.employeeId, endAddress, notes, points: finalPoints });
      setPoints([]); setEndAddress(''); setNotes(''); await query.refresh(); setFeedback('Fahrt abgeschlossen. Kilometer, Vergütung und Fahrzeitabzug wurden berechnet.');
    } catch (error) { setFeedback(error instanceof Error ? error.message : 'Fahrt konnte nicht abgeschlossen werden.'); } finally { setBusy(false); }
  }
  async function openMaps() {
    const origin = encodeURIComponent(startAddress || `${points[0]?.latitude ?? ''},${points[0]?.longitude ?? ''}`); const destination = encodeURIComponent(endAddress);
    if (!endAddress.trim()) { setFeedback('Bitte zuerst eine Zieladresse eintragen.'); return; }
    await Linking.openURL(`https://www.google.com/maps/dir/?api=1&origin=${origin}&destination=${destination}&travelmode=driving`);
  }

  if (!actor.isReady || query.loading && !query.data) return <PortalTabScreen title="Fahrtenbuch"><LoadingState message="Fahrtenbuch wird sicher geladen…" /></PortalTabScreen>;
  if (query.error && !query.data) return <PortalTabScreen title="Fahrtenbuch"><ErrorState title="Fahrtenbuch nicht verfügbar" message={query.error} onRetry={() => void query.refresh()} /></PortalTabScreen>;
  const bundle = query.data!;
  return <PortalTabScreen title="Fahrtenbuch" subtitle="GPS-Fahrten, Kilometervergütung, Belege und Tagesbestätigung" scroll>
    <View style={styles.page} testID="employee-logbook-screen">
      <PremiumCard variant="elevated" contentStyle={styles.hero}>
        <View style={styles.heroCopy}><Text style={styles.eyebrow}>DIGITALES FAHRTENBUCH</Text><Text style={styles.heroTitle}>{active ? 'Fahrt wird aufgezeichnet' : 'Bereit für die nächste Fahrt'}</Text><Text style={styles.heroText}>{active ? `${TRAVEL_ROUTE_TYPE_LABELS[active.routeType]} · gestartet ${new Date(active.startedAt).toLocaleTimeString('de-DE', { hour: '2-digit', minute: '2-digit' })}` : 'Automatische GPS-Kilometer, klare Fahrzeittrennung und direkte Gehaltsstatistik.'}</Text></View>
        <PremiumBadge label={active ? 'GPS AKTIV' : 'BEREIT'} variant={active ? 'green' : 'cyan'} />
      </PremiumCard>
      <InfoBanner variant="warning" message="Während der Fahrt: Gerät eingeschaltet lassen. Im Browser/PWA CareSuite nicht schließen und den Bildschirm nicht sperren. Standort und mobile Daten müssen aktiv sein. In der Android-App läuft die Aufzeichnung mit sichtbarer Dauerbenachrichtigung im Hintergrund weiter." />
      {feedback ? <InfoBanner variant={feedback.includes('nicht') || feedback.includes('Bitte') ? 'warning' : 'info'} message={feedback} /> : null}
      <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.tabs}>
        {([['record','Aufzeichnen'],['trips','Fahrten & PDF'],['receipts','Belege'],['profile','Fahrzeug & Führerschein']] as [Tab,string][]).map(([key,label]) => <Pressable key={key} onPress={() => setTab(key)} style={[styles.tab, tab === key && styles.tabActive]}><Text style={[styles.tabText, tab === key && styles.tabTextActive]}>{label}</Text></Pressable>)}
      </ScrollView>
      {tab === 'record' ? <RecordPanel active={active} tenantId={actor.tenantId} employeeId={actor.employeeId} routeType={routeType} setRouteType={setRouteType} purpose={purpose} setPurpose={setPurpose} assignmentId={assignmentId} setAssignmentId={setAssignmentId} manualReason={manualReason} setManualReason={setManualReason} startAddress={startAddress} setStartAddress={setStartAddress} endAddress={endAddress} setEndAddress={setEndAddress} notes={notes} setNotes={setNotes} busy={busy} begin={begin} finish={finish} openMaps={openMaps} setFeedback={setFeedback} /> : null}
      {tab === 'trips' ? <TripsPanel employeeName={actor.displayName} trips={bundle.trips} vehicles={bundle.vehicles} confirmations={bundle.confirmations.map((c) => c.workDate)} tenantId={actor.tenantId!} employeeId={actor.employeeId!} refresh={query.refresh} setFeedback={setFeedback} /> : null}
      {tab === 'receipts' ? <ReceiptsPanel tenantId={actor.tenantId!} employeeId={actor.employeeId!} trips={bundle.trips} setFeedback={setFeedback} /> : null}
      {tab === 'profile' ? <ProfilePanel tenantId={actor.tenantId!} employeeId={actor.employeeId!} profile={bundle.profile} vehicles={bundle.vehicles} refresh={query.refresh} setFeedback={setFeedback} /> : null}
    </View>
  </PortalTabScreen>;
}

function RecordPanel(p: any) { const [stopKind,setStopKind]=useState<'client'|'doctor'|'pharmacy'|'shopping'|'other'>('client'); const [stopLabel,setStopLabel]=useState(''); async function addStop(){try{await addLogbookStop({tenantId:p.tenantId,employeeId:p.employeeId,tripId:p.active.id,stopKind,label:stopLabel});setStopLabel('');p.setFeedback('Zwischenstopp mit genauer Uhrzeit gespeichert.');}catch(e){p.setFeedback(e instanceof Error?e.message:'Stopp konnte nicht gespeichert werden.');}} return <SectionPanel title={p.active ? 'Laufende Fahrt' : 'Fahrt starten'} subtitle="Einsatz zuordnen oder ohne Zuordnung verpflichtend begründen">
  {!p.active ? <View style={styles.form}><Text style={styles.label}>Fahrtart</Text><ScrollView horizontal contentContainerStyle={styles.chips}>{routes.map((key) => <Pressable key={key} onPress={() => p.setRouteType(key)} style={[styles.chip,p.routeType===key&&styles.chipActive]}><Text style={[styles.chipText,p.routeType===key&&styles.chipTextActive]}>{TRAVEL_ROUTE_TYPE_LABELS[key]}</Text></Pressable>)}</ScrollView><PremiumInput label="Zweck" value={p.purpose} onChangeText={p.setPurpose} /><View style={styles.cols}><PremiumInput label="Einsatz-ID (wenn vorhanden)" value={p.assignmentId} onChangeText={p.setAssignmentId} style={styles.grow} /><PremiumInput label="Begründung ohne Einsatz" value={p.manualReason} onChangeText={p.setManualReason} style={styles.grow} /></View><PremiumInput label="Startadresse (optional, GPS wird erfasst)" value={p.startAddress} onChangeText={p.setStartAddress} /><PremiumButton title="GPS-Fahrt verbindlich starten" size="lg" loading={p.busy} onPress={() => void p.begin()} /></View> : <View style={styles.form}><Text style={styles.label}>Zwischenstopp erfassen</Text><View style={styles.chips}>{([['client','Klient:in'],['doctor','Arzt'],['pharmacy','Apotheke'],['shopping','Einkauf'],['other','Sonstiges']] as const).map(([key,label])=><Pressable key={key} onPress={()=>setStopKind(key)} style={[styles.chip,stopKind===key&&styles.chipActive]}><Text style={styles.chipText}>{label}</Text></Pressable>)}</View><View style={styles.cols}><PremiumInput label="Name / Zweck des Stopps" value={stopLabel} onChangeText={setStopLabel} style={styles.grow}/><PremiumButton title="Stopp jetzt speichern" variant="secondary" onPress={()=>void addStop()}/></View><PremiumInput label="Zieladresse" value={p.endAddress} onChangeText={p.setEndAddress} /><PremiumInput label="Notiz" value={p.notes} onChangeText={p.setNotes} multiline /><View style={styles.actions}><PremiumButton title="Navigation in Google Maps öffnen" variant="secondary" onPress={() => void p.openMaps()} /><PremiumButton title="Fahrt abschließen" size="lg" loading={p.busy} onPress={() => void p.finish()} /></View><Text style={styles.privacy}>Google Maps erhält die von Ihnen gewählte Start-/Zielangabe nur beim Öffnen. CareSuite führt parallel die eigene Fahrtenaufzeichnung.</Text></View>}
  </SectionPanel>; }

function TripsPanel({ employeeName, trips, vehicles, confirmations, tenantId, employeeId, refresh, setFeedback }: { employeeName:string; trips:LogbookTrip[]; vehicles:LogbookVehicle[]; confirmations:string[]; tenantId:string; employeeId:string; refresh:()=>Promise<unknown>; setFeedback:(v:string|null)=>void }) {
  const [from,setFrom]=useState(today().slice(0,8)+'01'); const [to,setTo]=useState(today()); const [signDate,setSignDate]=useState(today()); const [signer,setSigner]=useState(employeeName); const [signature,setSignature]=useState(''); const [edit,setEdit]=useState<LogbookTrip|null>(null); const [km,setKm]=useState(''); const [reason,setReason]=useState('');
  async function confirm(){ try { await confirmLogbookDay({tenantId,employeeId,workDate:signDate,signerName:signer,signatureData:signature,trips}); await refresh(); setFeedback('Tagesfahrten wurden verbindlich unterschrieben.'); } catch(e){setFeedback(e instanceof Error?e.message:'Bestätigung fehlgeschlagen.');} }
  async function correct(){ if(!edit)return; try{await correctLogbookTrip(edit,Number(km.replace(',','.')),reason);setEdit(null);await refresh();setFeedback('Korrektur gespeichert. Die Änderung ist revisionssicher protokolliert.');}catch(e){setFeedback(e instanceof Error?e.message:'Korrektur fehlgeschlagen.');}}
  return <View style={styles.stack}><SectionPanel title="Zeitraum & PDF" subtitle="Vollständiger Nachweis für einen frei wählbaren Zeitraum"><View style={styles.cols}><PremiumInput label="Von" value={from} onChangeText={setFrom} style={styles.grow}/><PremiumInput label="Bis" value={to} onChangeText={setTo} style={styles.grow}/></View><PremiumButton title="Fahrtenbuch als PDF erstellen" onPress={()=>buildLogbookPdf({employeeName,from,to,trips,vehicles})}/></SectionPanel>
  <SectionPanel title="Meine Fahrten" subtitle="Korrekturen bleiben für Office vollständig nachvollziehbar">{!trips.length?<EmptyState title="Noch keine Fahrten" message="Starten Sie die erste Fahrt im Reiter Aufzeichnen."/>:trips.map(t=><PremiumCard key={t.id} style={styles.trip}><View style={styles.tripHead}><View style={styles.grow}><Text style={styles.tripTitle}>{TRAVEL_ROUTE_TYPE_LABELS[t.routeType]}</Text><Text style={styles.muted}>{new Date(t.startedAt).toLocaleString('de-DE')} · {t.purpose}</Text></View><PremiumBadge label={t.status==='recording'?'LÄUFT':t.correctedAt?'KORRIGIERT':'ERFASST'} variant={t.status==='recording'?'green':'cyan'}/></View><View style={styles.metrics}><Text style={styles.metric}>{t.distanceFinalKm.toFixed(2)} km</Text><Text style={styles.metric}>{(t.mileageAmountCents/100).toFixed(2)} EUR</Text><Text style={styles.metric}>{t.countsAsWorkTime?'Arbeitszeit':'Fahrzeitabzug '+t.worktimeDeductionMinutes+' Min.'}</Text></View>{t.status!=='recording'?<PremiumButton title="Eintrag korrigieren" size="sm" variant="ghost" onPress={()=>{setEdit(t);setKm(String(t.distanceFinalKm).replace('.',','));}}/>:null}</PremiumCard>)}</SectionPanel>
  {edit?<SectionPanel title="Fahrt korrigieren" subtitle="Die ursprünglichen Werte bleiben unsichtbar für Mitarbeitende, aber revisionssicher für Office erhalten"><PremiumInput label="Korrigierte Kilometer" value={km} onChangeText={setKm}/><PremiumInput label="Korrekturgrund (Pflicht)" value={reason} onChangeText={setReason}/><View style={styles.actions}><PremiumButton title="Abbrechen" variant="secondary" onPress={()=>setEdit(null)}/><PremiumButton title="Korrektur speichern" onPress={()=>void correct()}/></View></SectionPanel>:null}
  <SectionPanel title="Tagesabschluss unterschreiben" subtitle="Alle abgeschlossenen Fahrten des Tages verbindlich bestätigen"><PremiumInput label="Datum" value={signDate} onChangeText={setSignDate}/><PremiumInput label="Name" value={signer} onChangeText={setSigner}/>{confirmations.includes(signDate)?<InfoBanner message="Dieser Tag wurde bereits unterschrieben."/>:<><CareSignatureCanvas label="Unterschrift" onConfirm={setSignature}/><PremiumButton title="Tagesfahrten verbindlich bestätigen" disabled={!signature||!signer.trim()} onPress={()=>void confirm()}/></>}</SectionPanel></View>;
}

function ReceiptsPanel({tenantId,employeeId,trips,setFeedback}:{tenantId:string;employeeId:string;trips:LogbookTrip[];setFeedback:(v:string|null)=>void}){const [amount,setAmount]=useState('');const [date,setDate]=useState(today());const [category,setCategory]=useState('parking');const [tripId,setTripId]=useState<string|null>(null);const [file,setFile]=useState<DocumentPicker.DocumentPickerAsset|null>(null);async function choose(){const r=await DocumentPicker.getDocumentAsync({type:['image/*','application/pdf'],copyToCacheDirectory:true});if(!r.canceled)setFile(r.assets[0]);}async function save(){if(!file){setFeedback('Bitte zuerst einen Beleg auswählen.');return;}try{const path=await uploadLogbookFile({tenantId,employeeId,area:'receipts',uri:file.uri,fileName:file.name,mimeType:file.mimeType});await createLogbookReceipt({tenantId,employeeId,tripId,category,amountCents:Math.round(Number(amount.replace(',','.'))*100),expenseDate:date,storagePath:path,fileName:file.name,mimeType:file.mimeType});setFile(null);setAmount('');setFeedback('Beleg sicher gespeichert und der Fahrt zugeordnet.');}catch(e){setFeedback(e instanceof Error?e.message:'Beleg konnte nicht gespeichert werden.');}}return <SectionPanel title="Fahrtbeleg hochladen" subtitle="Parken, Maut, Kraftstoff oder sonstige dienstliche Auslagen"><View style={styles.chips}>{['parking','toll','fuel','other'].map(k=><Pressable key={k} onPress={()=>setCategory(k)} style={[styles.chip,category===k&&styles.chipActive]}><Text style={[styles.chipText,category===k&&styles.chipTextActive]}>{({parking:'Parken',toll:'Maut',fuel:'Kraftstoff',other:'Sonstiges'} as any)[k]}</Text></Pressable>)}</View><PremiumInput label="Datum" value={date} onChangeText={setDate}/><PremiumInput label="Betrag (EUR)" value={amount} onChangeText={setAmount}/><Text style={styles.label}>Fahrt zuordnen (optional)</Text><ScrollView horizontal contentContainerStyle={styles.chips}><Pressable onPress={()=>setTripId(null)} style={[styles.chip,!tripId&&styles.chipActive]}><Text style={styles.chipText}>Keine</Text></Pressable>{trips.slice(0,12).map(t=><Pressable key={t.id} onPress={()=>setTripId(t.id)} style={[styles.chip,tripId===t.id&&styles.chipActive]}><Text style={styles.chipText}>{new Date(t.startedAt).toLocaleDateString('de-DE')} · {t.purpose}</Text></Pressable>)}</ScrollView><View style={styles.actions}><PremiumButton title={file?file.name:'Beleg auswählen'} variant="secondary" onPress={()=>void choose()}/><PremiumButton title="Beleg speichern" onPress={()=>void save()}/></View></SectionPanel>}

function ProfilePanel({tenantId,employeeId,profile,vehicles,refresh,setFeedback}:{tenantId:string;employeeId:string;profile:any;vehicles:LogbookVehicle[];refresh:()=>Promise<unknown>;setFeedback:(v:string|null)=>void}){const [plate,setPlate]=useState(vehicles[0]?.plate??'');const [make,setMake]=useState(vehicles[0]?.make??'');const [model,setModel]=useState(vehicles[0]?.model??'');const [ownership,setOwnership]=useState<'private'|'company'>(vehicles[0]?.ownership??'private');const [rate,setRate]=useState(String(profile.mileageRateCents/100).replace('.',','));async function saveVehicle(){try{await saveLogbookVehicle({id:vehicles[0]?.id,tenantId,employeeId,plate,make,model,ownership,active:true});await saveLogbookProfile({...profile,mileageRateCents:Math.round(Number(rate.replace(',','.'))*100),gpsConsent:true,defaultVehicleId:vehicles[0]?.id??null});await refresh();setFeedback('Fahrzeug, Kilometersatz und GPS-Einwilligung gespeichert.');}catch(e){setFeedback(e instanceof Error?e.message:'Speichern fehlgeschlagen.');}}async function license(side:'front'|'back'){const r=await DocumentPicker.getDocumentAsync({type:['image/*','application/pdf'],copyToCacheDirectory:true});if(r.canceled)return;try{const a=r.assets[0];const path=await uploadLogbookFile({tenantId,employeeId,area:'license',uri:a.uri,fileName:a.name,mimeType:a.mimeType});await saveLogbookProfile({...profile,[side==='front'?'licenseFrontPath':'licenseBackPath']:path});await refresh();setFeedback(`Führerschein-${side==='front'?'Vorderseite':'Rückseite'} sicher gespeichert.`);}catch(e){setFeedback(e instanceof Error?e.message:'Upload fehlgeschlagen.');}}return <View style={styles.stack}><SectionPanel title="Fahrzeug & Kilometersatz" subtitle="Privat- oder Firmenfahrzeug und individuelle Erstattung"><View style={styles.chips}>{(['private','company'] as const).map(k=><Pressable key={k} onPress={()=>setOwnership(k)} style={[styles.chip,ownership===k&&styles.chipActive]}><Text style={styles.chipText}>{k==='private'?'Privatfahrzeug':'Firmenfahrzeug'}</Text></Pressable>)}</View><View style={styles.cols}><PremiumInput label="Kennzeichen" value={plate} onChangeText={setPlate} style={styles.grow}/><PremiumInput label="Hersteller" value={make??''} onChangeText={setMake} style={styles.grow}/><PremiumInput label="Modell" value={model??''} onChangeText={setModel} style={styles.grow}/><PremiumInput label="EUR je km" value={rate} onChangeText={setRate} style={styles.grow}/></View><PremiumButton title="Fahrzeugdaten speichern" onPress={()=>void saveVehicle()}/></SectionPanel><SectionPanel title="Führerschein" subtitle="Vorder- und Rückseite geschützt in der Personalakte hinterlegen"><View style={styles.actions}><PremiumButton title={profile.licenseFrontPath?'Vorderseite ersetzen':'Vorderseite hochladen'} variant="secondary" onPress={()=>void license('front')}/><PremiumButton title={profile.licenseBackPath?'Rückseite ersetzen':'Rückseite hochladen'} variant="secondary" onPress={()=>void license('back')}/></View></SectionPanel></View>}

const styles=StyleSheet.create({page:{width:'100%',gap:careSpacing.md},stack:{gap:careSpacing.md},hero:{minHeight:150,flexDirection:'row',alignItems:'center',justifyContent:'space-between',gap:careSpacing.lg},heroCopy:{flex:1,gap:6},eyebrow:{...typography.caption,color:portalPremium.accent.blue,fontWeight:'800',letterSpacing:1.4},heroTitle:{...typography.h1,color:portalPremium.text.primary},heroText:{...typography.body,color:portalPremium.text.secondary,maxWidth:760},tabs:{gap:8,paddingVertical:4},tab:{minHeight:44,justifyContent:'center',paddingHorizontal:18,borderRadius:14,borderWidth:1,borderColor:portalPremium.borderSoft,backgroundColor:portalPremium.surfaceRaised},tabActive:{borderColor:portalPremium.accent.blue,backgroundColor:portalPremium.surfaceMuted},tabText:{...typography.body,color:portalPremium.text.secondary,fontWeight:'700'},tabTextActive:{color:portalPremium.accent.blueDark},form:{gap:careSpacing.md},label:{...typography.label,color:portalPremium.text.primary},chips:{flexDirection:'row',flexWrap:'wrap',gap:8},chip:{minHeight:38,justifyContent:'center',paddingHorizontal:13,borderRadius:999,borderWidth:1,borderColor:portalPremium.borderSoft,backgroundColor:portalPremium.surfaceRaised},chipActive:{borderColor:portalPremium.accent.blue,backgroundColor:portalPremium.surfaceMuted},chipText:{...typography.caption,color:portalPremium.text.secondary,fontWeight:'700'},chipTextActive:{color:portalPremium.accent.blueDark},cols:{flexDirection:'row',flexWrap:'wrap',gap:careSpacing.sm},grow:{flex:1,minWidth:220},actions:{flexDirection:'row',flexWrap:'wrap',gap:careSpacing.sm,alignItems:'center'},privacy:{...typography.caption,color:portalPremium.text.muted},trip:{marginBottom:careSpacing.sm},tripHead:{flexDirection:'row',alignItems:'center',justifyContent:'space-between',gap:careSpacing.sm},tripTitle:{...typography.h3,color:portalPremium.text.primary},muted:{...typography.caption,color:portalPremium.text.muted},metrics:{flexDirection:'row',flexWrap:'wrap',gap:8,marginVertical:10},metric:{...typography.caption,color:portalPremium.text.primary,backgroundColor:portalPremium.surfaceSoft,borderRadius:999,paddingHorizontal:10,paddingVertical:6},});
