# CareSuite+ — Umgebungsvariablen (Beispiel, keine Secrets committen)
# Pilot-Deployment: Datei als `.env` kopieren und Werte eintragen.

# --- Supabase (Frontend: nur Publishable/Anon Key, niemals service_role) ---
# Projekt-URL aus Supabase Dashboard → Settings → API
EXPO_PUBLIC_SUPABASE_URL=
# Publishable Key (empfohlen) — nur für Client-App, nicht für Server-Secrets
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Legacy-Anon-Key (optional, falls Publishable Key noch nicht gesetzt)
# EXPO_PUBLIC_SUPABASE_ANON_KEY=

# --- Auth- und Service-Modus ---
# true  = Demo-Login + In-Memory-Daten (nur lokale Entwicklung)
# false = Live-Supabase: Passwort-Login, RLS, kein DEMO_TENANT_ID-Fallback (Standard)
EXPO_PUBLIC_DEMO_MODE=false

# --- Live-Pilot (EXPO_PUBLIC_DEMO_MODE=false) ---
# EXPO_PUBLIC_DEMO_MODE=false
# EXPO_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
# EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_<key>
#
# Voraussetzungen:
# - Migrationen per `supabase db push` (lokal oder Remote)
# - Mandanten + Auth-User + profile.tenant_id gesetzt
# - Kein service_role, sb_secret, DATABASE_URL oder OPENAI_API_KEY im Frontend
# - OPENAI_API_KEY nur als Supabase Edge Function Secret
#
# Typen generieren (Remote, nur mit Zugangsdaten):
#   npx supabase login
#   npx supabase link --project-ref <ref>
#   npx supabase gen types typescript --linked > src/lib/supabase/types.ts
#
# Quality Gates:
#   npm run typecheck && npm run test && npm run smoke
#   npm run platform:audit && npm run store:audit

# --- EAS Build (optional, für eas build / submit) ---
# UUID aus `npx eas project:init` — nicht erfinden
# EAS_PROJECT_ID=
# APP_ENV wird durch eas.json Profile gesetzt (development|preview|production)
