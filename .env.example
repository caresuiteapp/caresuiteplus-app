# --- Kartenansicht ---
# Google Maps (bevorzugt für Live-Tracking im Browser):
# EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
# Optional: Mapbox für statische Kacheln
# EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=
# MAPBOX_ACCESS_TOKEN=

# --- Google Maps Fahrzeit (serverseitig) ---
# Supabase Edge Function Secret (Dashboard → Edge Functions → Secrets):
# GOOGLE_MAPS_API_KEY=
# Optional/reserviert für spätere Kartenkacheln (Travel Time nutzt nur die Edge Function):
# EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=  # Browser-Key für Live-Karte (Maps JavaScript API)
