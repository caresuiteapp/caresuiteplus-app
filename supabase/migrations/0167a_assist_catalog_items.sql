-- ==========================================================================
-- CareSuite+ — Migration 0167a: Assist catalog_items (monolith stub)
-- Seeds are applied via 0167_assist_catalog_items_and_template_entries.sql
-- and split files 0167a0..0167a4 (valid UUIDs). This stub preserves filename order.
-- Former monolith used truncated a1000000-* UUIDs (invalid for Postgres uuid type).
-- ==========================================================================

SELECT 1;
